import React from "react";
import { motion } from "motion/react";
import { twMerge } from "tailwind-merge";

const RevealBento = () => {
  return (
    <div className="min-h-screen bg-zinc-900 py-12 text-zinc-50">
      RevealBento
    </div>
  );
};

export default RevealBento;
